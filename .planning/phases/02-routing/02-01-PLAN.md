---
phase: 02-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/agent-inbox-router/SKILL.md
  - skills/orchestrator/SKILL.md
  - AGENTS.md
  - lib/agent-cron.sh
autonomous: true
requirements: [ROUTE-01, ROUTE-02]

must_haves:
  truths:
    - "inbox-router SKILL.md declara explicitamente ser o único conversor Obsidian→handoff"
    - "orchestrator SKILL.md não menciona routing de inbox"
    - "AGENTS.md não atribui routing de inbox ao orchestrator"
    - "agent-cron.sh comment reflects that inbox-router skill does the routing"
  artifacts:
    - path: "skills/agent-inbox-router/SKILL.md"
      provides: "Declaração de responsabilidade exclusiva Obsidian→handoff"
      contains: "único responsável"
    - path: "skills/orchestrator/SKILL.md"
      provides: "Orchestrator sem menção a inbox routing"
    - path: "AGENTS.md"
      provides: "Orchestrator description sem inbox routing"
  key_links:
    - from: "skills/agent-inbox-router/SKILL.md"
      to: "lib/agent-cron.sh"
      via: "cron invokes task-manager with inbox-router skill"
      pattern: "inbox-router"
---

<objective>
Clarificar que inbox-router é o único conversor Obsidian→handoff, removendo sobreposição de responsabilidade do orchestrator e task-manager.

Purpose: Eliminar ambiguidade de ownership no routing de inbox — evita execução duplicada ou confusa.
Output: Documentação atualizada em 4 arquivos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declarar inbox-router como único conversor e limpar orchestrator</name>
  <read_first>
    - skills/agent-inbox-router/SKILL.md
    - skills/orchestrator/SKILL.md
  </read_first>
  <files>skills/agent-inbox-router/SKILL.md, skills/orchestrator/SKILL.md</files>
  <action>
1. Em `skills/agent-inbox-router/SKILL.md`: adicionar frase explícita no topo da seção de responsabilidades: "Este skill é o **único responsável** por converter notas do Obsidian inbox em handoffs. Nenhum outro agente ou skill deve fazer esta conversão." (per ROUTE-01)

2. Em `skills/orchestrator/SKILL.md`: remover a seção "Roteamento de Inbox Local" (linhas ~28-42). Se houver qualquer menção a "inbox", "obsidian routing" ou "roteamento de inbox" fora dessa seção, remover também. (per ROUTE-02)
  </action>
  <verify>
    <automated>grep -c "único responsável" skills/agent-inbox-router/SKILL.md && ! grep -qi "roteamento de inbox" skills/orchestrator/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "único responsável" skills/agent-inbox-router/SKILL.md retorna match
    - grep -i "roteamento de inbox" skills/orchestrator/SKILL.md retorna 0 matches
    - grep -i "rotear inbox" skills/orchestrator/SKILL.md retorna 0 matches
  </acceptance_criteria>
  <done>inbox-router declara responsabilidade exclusiva; orchestrator não menciona inbox routing</done>
</task>

<task type="auto">
  <name>Task 2: Limpar AGENTS.md e comentário do cron</name>
  <read_first>
    - AGENTS.md
    - lib/agent-cron.sh
  </read_first>
  <files>AGENTS.md, lib/agent-cron.sh</files>
  <action>
1. Em `AGENTS.md` linha ~245: alterar "Responsável por rodar o scheduler, rotear inbox Obsidian e spawnar subagentes" para "Responsável por rodar o scheduler e spawnar subagentes" — remover "rotear inbox Obsidian". (per ROUTE-02)

2. Em `lib/agent-cron.sh` linhas ~139-153: alterar o comentário "Obsidian inbox → roteamento pelo task-manager" para "Obsidian inbox → conversão pelo skill inbox-router (executado via task-manager)". O código não muda, apenas o comentário. (per ROUTE-01)
  </action>
  <verify>
    <automated>! grep -q "rotear inbox Obsidian" AGENTS.md && grep -q "inbox-router" lib/agent-cron.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "rotear inbox Obsidian" AGENTS.md retorna 0 matches
    - grep "inbox-router" lib/agent-cron.sh retorna match no comentário da seção de cron
    - grep "roteamento pelo task-manager" lib/agent-cron.sh retorna 0 matches
  </acceptance_criteria>
  <done>AGENTS.md não atribui inbox routing ao orchestrator; cron comment reflete que inbox-router faz a conversão</done>
</task>

</tasks>

<verification>
1. `grep -r "único responsável.*Obsidian" skills/agent-inbox-router/` — confirma declaração exclusiva
2. `grep -ri "rotea.*inbox" skills/orchestrator/ AGENTS.md` — retorna vazio (sem sobreposição)
3. `grep "inbox-router" lib/agent-cron.sh` — confirma comentário correto
</verification>

<success_criteria>
- inbox-router SKILL.md declara ser o único conversor Obsidian→handoff
- orchestrator SKILL.md e AGENTS.md não mencionam routing de inbox
- agent-cron.sh comment atribui conversão ao inbox-router skill
</success_criteria>

<output>
After completion, create `.planning/phases/02-routing/02-01-SUMMARY.md`
</output>
