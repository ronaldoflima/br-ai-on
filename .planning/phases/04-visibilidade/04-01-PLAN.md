---
phase: 04-visibilidade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/app/lib/types.ts
  - dashboard/app/api/handoffs/route.ts
  - dashboard/app/handoffs/page.tsx
autonomous: true
requirements:
  - VIS-01

must_haves:
  truths:
    - "Dashboard exibe thread_id quando presente no handoff"
    - "Handoffs sem thread_id continuam exibindo normalmente"
    - "Dashboard compila sem erros TypeScript"
  artifacts:
    - path: "dashboard/app/lib/types.ts"
      provides: "Handoff interface com thread_id"
      contains: "thread_id"
    - path: "dashboard/app/api/handoffs/route.ts"
      provides: "parseHandoff retorna thread_id"
      contains: "thread_id"
    - path: "dashboard/app/handoffs/page.tsx"
      provides: "UI mostra thread_id como badge"
      contains: "thread_id"
  key_links:
    - from: "dashboard/app/api/handoffs/route.ts"
      to: "handoff YAML files"
      via: "meta.thread_id parsed from frontmatter"
      pattern: "thread_id.*meta\\.thread_id"
    - from: "dashboard/app/handoffs/page.tsx"
      to: "Handoff interface"
      via: "h.thread_id rendered in UI"
      pattern: "thread_id"
---

<objective>
Adicionar thread_id à visualização de handoffs no dashboard.

Purpose: Phase 3 adicionou thread_id ao schema de handoffs mas o dashboard não exibe esse campo ainda.
Output: Dashboard mostra thread_id como badge quando presente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@dashboard/app/lib/types.ts
@dashboard/app/api/handoffs/route.ts
@dashboard/app/handoffs/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar thread_id ao tipo e API de handoffs</name>
  <files>dashboard/app/lib/types.ts, dashboard/app/api/handoffs/route.ts</files>
  <read_first>
    - dashboard/app/lib/types.ts (Handoff interface, line ~46)
    - dashboard/app/api/handoffs/route.ts (Handoff interface line ~11, parseHandoff line ~40)
  </read_first>
  <action>
    1. In `dashboard/app/lib/types.ts`, add `thread_id?: string | null;` to the Handoff interface after `reply_to` (line 53, before `description`).

    2. In `dashboard/app/api/handoffs/route.ts`:
       - Add `thread_id?: string | null;` to the local Handoff interface after `reply_to` (line 18, before `description`).
       - In `parseHandoff()` return object (around line 49), add `thread_id: meta.thread_id || null,` after `reply_to`.
  </action>
  <acceptance_criteria>
    - grep -q "thread_id" dashboard/app/lib/types.ts
    - grep -q "thread_id" dashboard/app/api/handoffs/route.ts
    - grep -q "meta.thread_id" dashboard/app/api/handoffs/route.ts
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ronaldo.limapx.center/br-ai-on/dashboard && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Handoff interface tem thread_id em ambos os arquivos, parseHandoff extrai do YAML, TypeScript compila sem erros</done>
</task>

<task type="auto">
  <name>Task 2: Exibir thread_id na UI de handoffs</name>
  <files>dashboard/app/handoffs/page.tsx</files>
  <read_first>
    - dashboard/app/handoffs/page.tsx (around line 420-440 where reply_to is rendered)
  </read_first>
  <action>
    In `dashboard/app/handoffs/page.tsx`, find where `reply_to` is displayed (around line 426). After the reply_to badge, add a conditional thread_id badge:

    When `h.thread_id` is present, render a small inline element with text like "thread: {h.thread_id}" styled similarly to the reply_to badge but with a distinct color (e.g., purple/violet tones). Use the same Tailwind pattern as existing badges in the file.

    Example pattern (adapt to match existing style):
    ```
    {h.thread_id && (
      <span className="text-xs text-purple-400">
        thread: {h.thread_id}
      </span>
    )}
    ```
  </action>
  <acceptance_criteria>
    - grep -q "thread_id" dashboard/app/handoffs/page.tsx
    - grep -q "thread" dashboard/app/handoffs/page.tsx
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ronaldo.limapx.center/br-ai-on/dashboard && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>thread_id aparece como badge na UI quando presente, handoffs sem thread_id exibem normalmente</done>
</task>

</tasks>

<verification>
1. `cd dashboard && npx tsc --noEmit` — sem erros TypeScript
2. `grep -c "thread_id" dashboard/app/lib/types.ts` — pelo menos 1
3. `grep -c "thread_id" dashboard/app/api/handoffs/route.ts` — pelo menos 2
4. `grep -c "thread_id" dashboard/app/handoffs/page.tsx` — pelo menos 1
</verification>

<success_criteria>
- Dashboard compila sem erros
- thread_id presente no tipo Handoff, na API parseHandoff, e na UI
- Handoffs existentes sem thread_id continuam funcionando
</success_criteria>

<output>
After completion, create `.planning/phases/04-visibilidade/04-01-SUMMARY.md`
</output>
