---
phase: 03-schema-de-handoffs
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - skills/agent-handoff/SKILL.md
autonomous: true
requirements:
  - SCHEMA-04
must_haves:
  truths:
    - "SKILL.md instrui agente a verificar thread_id no handoff"
    - "SKILL.md instrui agente a carregar thread-history antes de processar"
  artifacts:
    - path: "skills/agent-handoff/SKILL.md"
      provides: "thread_id awareness instructions"
      contains: "thread-history"
  key_links:
    - from: "skills/agent-handoff/SKILL.md step 3c"
      to: "lib/handoff.sh thread-history"
      via: "bash command in instruction"
      pattern: "thread-history"
---

<objective>
Atualizar SKILL.md do agent-handoff para instruir agentes a carregar thread history quando thread_id esta presente.

Purpose: Agentes ganham contexto de conversacao ao processar handoffs com thread
Output: skills/agent-handoff/SKILL.md atualizado com instrucoes de thread
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/agent-handoff/SKILL.md
@.planning/phases/03-schema-de-handoffs/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md with thread_id awareness (SCHEMA-04)</name>
  <files>skills/agent-handoff/SKILL.md</files>
  <read_first>skills/agent-handoff/SKILL.md</read_first>
  <action>
Modify step 3c in skills/agent-handoff/SKILL.md. Currently step 3c says:

"**c) Leia o arquivo** (frontmatter + corpo) e identifique: `from`, `expects`, `description`, `context`, `expected`"

Update to:

"**c) Leia o arquivo** (frontmatter + corpo) e identifique: `from`, `expects`, `description`, `context`, `expected`, `thread_id`

Se `thread_id` estiver presente no frontmatter, carregue o historico da thread antes de processar:
```bash
thread_ctx=$(bash "$BRAION/lib/handoff.sh" thread-history "<thread_id>")
```
Use `$thread_ctx` como contexto adicional para entender o historico da conversa antes de executar a acao."

Also update step 3e (the reply/send section) to mention passing thread_id when replying:

After the existing send command example, add a note:
"Se o handoff original tinha `thread_id`, passe-o como parametro 8 ao responder:
```bash
bash "$BRAION/lib/handoff.sh" send "$AGENT" "<from>" info "<ho_id>" "<resumo>" "<resultado>" "<proximos passos>" "<thread_id>"
```
Isso garante que a thread continua rastreavel."
  </action>
  <verify>
    <automated>
cd /Users/ronaldo.limapx.center/br-ai-on && grep -q "thread-history" skills/agent-handoff/SKILL.md && grep -q "thread_id" skills/agent-handoff/SKILL.md && echo "PASS: SKILL.md has thread instructions" || echo "FAIL: missing thread instructions"
    </automated>
  </verify>
  <acceptance_criteria>
- grep "thread-history" skills/agent-handoff/SKILL.md returns match
- grep "thread_id" skills/agent-handoff/SKILL.md returns match
- step 3c mentions loading thread history when thread_id present
  </acceptance_criteria>
  <done>SKILL.md instructs agents to load thread history and pass thread_id when replying</done>
</task>

</tasks>

<verification>
grep for "thread-history" and "thread_id" in SKILL.md confirms instructions are present.
</verification>

<success_criteria>
- SKILL.md step 3c checks for thread_id and loads thread-history
- SKILL.md step 3e instructs passing thread_id when replying
</success_criteria>

<output>
After completion, create `.planning/phases/03-schema-de-handoffs/03-02-SUMMARY.md`
</output>
