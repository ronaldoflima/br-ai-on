---
phase: 01-limpeza-de-canais
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/orchestrator/SKILL.md
  - lib/orchestrate.sh
  - .gitignore
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02

must_haves:
  truths:
    - "Nenhum documento de skill instrui agentes a usar task_board.md ou messages.jsonl"
    - "Nenhum script define variáveis ou funções que escrevem em task_board.md ou messages.jsonl"
    - "task_board.md e messages.jsonl não aparecem no .gitignore (já não existem)"
  artifacts:
    - path: "skills/orchestrator/SKILL.md"
      provides: "Orchestrator skill sem referências a canais deprecated"
      contains: "handoff"
    - path: "lib/orchestrate.sh"
      provides: "Script sem funções de task_board/messages"
  key_links:
    - from: "skills/orchestrator/SKILL.md"
      to: "lib/handoff.sh"
      via: "Instruções de distribuição via handoff"
      pattern: "handoff"
---

<objective>
Remover todas as referências ativas a task_board.md e messages.jsonl do código e documentação de skills.

Purpose: Os arquivos já foram deletados; este plano limpa referências remanescentes para que nenhum agente tente usar canais deprecated.
Output: SKILL.md do orchestrator reescrito para usar handoffs, lib/orchestrate.sh limpo, .gitignore atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever SKILL.md do orchestrator para usar handoffs</name>
  <files>skills/orchestrator/SKILL.md</files>
  <read_first>skills/orchestrator/SKILL.md, lib/handoff.sh</read_first>
  <action>
Reescrever as seções do SKILL.md que referenciam task_board.md e messages.jsonl para usar handoffs como mecanismo de comunicação. Mudanças específicas:

1. **Seção "Fluxo de Orquestração" (linha 24)**: Trocar passo 4 de "Distribuir via agents/shared/task_board.md" para "Distribuir via handoffs (lib/handoff.sh create)"

2. **Seção "Criação de Tarefas" (linhas 44-58)**: Substituir todo o bloco de template task_board.md por instruções de criar handoff. O novo template deve usar `lib/handoff.sh create` com campos: from, to, type, subject, body. Manter o mesmo nível de detalhe (prioridade, contexto, detalhes).

3. **Seção "Mensagens Diretas" (linhas 79-84)**: Remover completamente esta seção. Comunicação urgente também vai via handoff com type=urgent.

4. **Seção "Consolidação" (linha 93)**: Remover referência a "shared/archive/" — handoffs têm seu próprio ciclo de arquivamento.

Manter todas as outras seções intactas (Agentes Disponíveis, Scoped Handoffs, Regras de Distribuição, Controle de Concorrência, Roteamento de Inbox Local).
  </action>
  <verify>
    <automated>! grep -q "task_board.md" skills/orchestrator/SKILL.md && ! grep -q "messages.jsonl" skills/orchestrator/SKILL.md && grep -q "handoff" skills/orchestrator/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "task_board.md" skills/orchestrator/SKILL.md retorna vazio
    - grep "messages.jsonl" skills/orchestrator/SKILL.md retorna vazio
    - grep "handoff" skills/orchestrator/SKILL.md retorna pelo menos 3 matches
    - Seção "Fluxo de Orquestração" existe e menciona handoff
    - Seção "Criação de Tarefas" existe com template usando handoff
    - Seção "Mensagens Diretas" não existe mais
  </acceptance_criteria>
  <done>SKILL.md do orchestrator não contém referências a task_board.md ou messages.jsonl; todas as instruções de comunicação usam handoffs</done>
</task>

<task type="auto">
  <name>Task 2: Limpar lib/orchestrate.sh e .gitignore</name>
  <files>lib/orchestrate.sh, .gitignore</files>
  <read_first>lib/orchestrate.sh, .gitignore</read_first>
  <action>
1. **lib/orchestrate.sh**: Remover as variáveis BOARD_FILE e MSG_FILE (linhas 6-7). Remover as funções que dependem delas:
   - `create_task` (linhas 9-35) — escrevia no task_board.md
   - `send_message` (linhas 37-59) — escrevia no messages.jsonl
   - `read_messages` (linhas 61-64) — lia do messages.jsonl
   
   Manter: header (set -euo pipefail), AGENTS_DIR, SHARED_DIR, e a função `list_pending` (linhas 66-73) se ela puder funcionar sem BOARD_FILE — caso contrário, remover também e deixar apenas o shell header com comentário de que funções foram migradas para handoffs.
   
   Na prática, `list_pending` usa BOARD_FILE, então deve ser removida também. O arquivo final deve conter apenas o header bash e um comentário: `# Funções de orquestração migradas para handoffs (lib/handoff.sh)` seguido de `# Este arquivo mantido para backwards compatibility — será removido em versão futura.`

2. **.gitignore**: Remover a linha `agents/shared/messages.jsonl` (linha 23). O arquivo já não existe e não será recriado.
  </action>
  <verify>
    <automated>! grep -q "task_board" lib/orchestrate.sh && ! grep -q "messages.jsonl" lib/orchestrate.sh && ! grep -q "messages.jsonl" .gitignore && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "BOARD_FILE" lib/orchestrate.sh retorna vazio
    - grep "MSG_FILE" lib/orchestrate.sh retorna vazio
    - grep "messages.jsonl" .gitignore retorna vazio
    - lib/orchestrate.sh ainda é válido bash (bash -n lib/orchestrate.sh retorna 0)
    - lib/orchestrate.sh contém comentário sobre migração para handoffs
  </acceptance_criteria>
  <done>lib/orchestrate.sh não contém funções de task_board/messages; .gitignore não referencia messages.jsonl</done>
</task>

</tasks>

<verification>
Verificação completa da fase:
1. `grep -r "task_board.md" skills/ lib/` — deve retornar vazio
2. `grep -r "messages.jsonl" skills/ lib/ .gitignore` — deve retornar vazio
3. `bash -n lib/orchestrate.sh` — deve passar (sintaxe válida)
4. `grep "handoff" skills/orchestrator/SKILL.md | wc -l` — deve ser >= 3
</verification>

<success_criteria>
- Zero referências a task_board.md em skills/ e lib/
- Zero referências a messages.jsonl em skills/, lib/, e .gitignore
- Orchestrator SKILL.md instrui agentes a usar handoffs para toda comunicação
- lib/orchestrate.sh é bash válido sem funções deprecated
</success_criteria>

<output>
After completion, create `.planning/phases/01-limpeza-de-canais/01-01-SUMMARY.md`
</output>
