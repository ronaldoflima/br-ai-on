# Segredo TOTP para autenticação (gerado por scripts/setup-totp.ts)
TOTP_SECRET=

# Desabilita autenticação OTP — acesso direto ao dashboard sem senha
# ATENÇÃO: use apenas em ambientes locais/confiáveis
DISABLE_AUTH=false
